import Rand
import duck
Rand.seed(3)
for i = 0 to 10 do
  duck.println(Rand.rand())
loop
duck.println("--------------")
for i = 0 to 10 do
  duck.println(Rand.boundedrand(0, 100))
loop
duck.println("--------------")
for i = 0 to 10 do
  duck.println(Rand.randfloat(0, 100))
loop
